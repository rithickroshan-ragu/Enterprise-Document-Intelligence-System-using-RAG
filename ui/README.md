# Minimal React Chat UI (Vite)

Run the UI locally:

```bash
cd ui
npm install
npm run dev
```

This is a minimal, local scaffold. Replace the placeholder echo logic in `src/App.jsx` with a fetch to your backend API to connect to the RAG service.
